# Product

<!-- impeccable:product-schema 1 -->

> 이 파일은 impeccable(디자인 가드) 이 읽는 제품 사실 기록이다(TASK-112). 사람용 개요는 README.md,
> 에이전트 작업 지침은 AGENTS.md, 캐릭터 설정은 `backlog/docs/doc-3` 이 정본이며 여기서 중복하지 않는다.
> 인터뷰 없이 저장소 문서(AGENTS.md·README.md·doc-3)에서 도출했다 — 자율 드레인(`/loop-backlog`) 중
> 작성이라 대화 라운드를 열지 않았고, 근거 없는 항목은 만들지 않고 비워 두었다.

## Platform

web

## Users

읽는 쪽은 한국어권 기술 실무자다. AI 툴·터미널·개발 워크플로를 이미 어느 정도 아는 사람으로 상정하며,
자명한 배경 설명 없이 결론과 수치를 바로 읽고 싶어 한다. 대개 링크(Slack·SNS·피드)를 타고 한 페이지에
들어와 그것만 읽고 나가거나, 루트 갤러리에서 제목을 훑어 관심 있는 것을 고른다.

쓰는 쪽은 사이트 주인 한 명(kil9)과 그를 대신해 페이지를 만드는 AI 에이전트다. 에이전트가 실제 저자이므로,
이 저장소의 문서는 사람이 읽는 안내문이자 에이전트가 따르는 실행 지침을 겸한다.

## Product Purpose

`today i learned` 는 조사·정리한 결과를 한 페이지짜리 자체 완결형 HTML 로 발행해 쌓는 개인 아카이브다.
성공은 발행량이 아니라 **한 페이지가 그 자체로 완결되는 것** — 링크 하나만 넘겨도 맥락·수치·근거가 다 있고,
5년 뒤에 열어도 외부 의존이 죽어 깨지지 않는 상태다.

## Positioning

빌드 단계가 없다. 정적 HTML 파일이 곧 배포물이고, 각 페이지는 CSS·이미지·차트를 전부 인라인한 단일 파일이다
(사이드카 예외는 `og/`·`p/archive/thumbs/` 뿐 — 남의 크롤러가 읽어 가므로 `data:` URI 를 못 쓴다).
프레임워크·번들러·웹폰트·CDN 이 없으므로 페이지의 수명이 어떤 의존성의 수명보다 길다.

또 하나는 화자다. 이 사이트의 보고서는 대부분 AI 가 쓰는데, 그 사실을 숨기지 않고 **리브**라는 전속 AI
지식 큐레이터 캐릭터를 화자로 세워 드러낸다.

## Operating Context

- 호스팅: GitHub Pages 가 `main` 브랜치 루트를 그대로 서빙한다(빌드 없음). 커스텀 도메인 til.kil9.dev.
- 발행: 에이전트가 페이지를 만들고 루트 갤러리 카드·README 표를 갱신한 뒤, 생성물 스크립트
  (`archive-thumbs.py` → `relink-pages.py` → `site-feed.py` → `search-index.py`)를 순서대로 돌리고
  `site-check.py` 로 점검한 다음 push 한다. 상세는 AGENTS.md 퍼블리시 런북이 정본이다.
- 진실원본: 루트 `index.html` 의 갤러리 카드. 피드·sitemap·검색 색인·썸네일·OG·이전/다음 내비·절 앵커는
  전부 거기서 파생된 생성물이라 손으로 고치지 않는다. 각 페이지의 `<!-- PAGEOG -->`·`<!-- PAGENAV -->`·
  `<!-- PAGETOC -->` 마커 구간도 스크립트가 통째로 덮어쓴다.
- 별도 자동 발행 계통으로 `p/briefing/`(리브의 아침 브리핑)이 있다. 머신 로컬 잡이 매일 렌더·push 하며
  갤러리·README·아카이브에 노출하지 않고 `noindex, follow` 로 나간다.
- 진행 관리는 `backlog/`(backlog CLI). 글감·초안(`topics/`·`drafts/`)은 저장소가 public 이라 비커밋이다.

## Capabilities and Constraints

- 저장소가 **public** 이다. push 하는 순간 공개되고 히스토리에 남는다. 시크릿·비공개 정보를 담지 않는다.
- 페이지는 외부 리소스 의존 없이 단일 파일로 열려야 한다. 웹폰트를 쓰지 않고 시스템 폰트 스택만 쓴다.
  차트·다이어그램은 인라인 SVG, 삽화·스티커·아바타는 base64 `data:image/webp` 로 임베드한다.
- 유일하게 허용된 외부 스크립트는 Cloudflare Web Analytics beacon 하나다(쿠키 없는 익명 집계, 실패해도
  렌더에 영향 없음). 그 외 추적·텔레메트리 주입 스크립트는 제거한다.
- 라이트/다크 모두 지원해야 한다. `prefers-color-scheme` 이 유일한 테마 신호이며 토글 UI 는 없다.
- URL 은 영구다. 슬러그(`<YYYY>/<slug>/`, `p/<slug>/`)는 발행 전에 사람에게 확인받고, 구 URL 은
  `404.html` 리다이렉트 맵이 받는다. 반면 주제 분류(`data-topic`)는 URL 에 박히지 않아 언제든 재조정한다.
- 페이지 HTML 상한 5MB(`site-check.py` 가 강제). 용량 최적화 자체가 목표는 아니고, 압축 누락·중복 임베드
  같은 사고를 발행 전에 잡는 것이 목적이다. 브리핑 회차는 별도로 500KB 상한.
- 자바스크립트는 루트 갤러리(검색·주제 칩·월별 목차·정렬)와 아카이브 페이지에만 있고, 개별 아티클에는
  사실상 없다. JS 가 죽어도 본문은 읽힌다.

## Brand Commitments

- 사이트 이름은 소문자 `today i learned`.
- 화자는 전속 AI 지식 큐레이터 **리브**(풀네임 "리브 투데이"는 소개·설정화 페이지에서만 노출). 성격은
  "일은 정확하게 하되 두 번 하기 싫어 최소 동선을 찾는 사람" — 나른하되 무기력하지 않다. 존댓말을 쓰고
  사이트 주인을 "관리자님"으로 부른다. 설정 정본은 `backlog/docs/doc-3`, 적용 규칙 요약은 AGENTS.md §2-2.
- 캐릭터 자산은 굵은 아웃라인의 플랫 마스코트 화풍이다. 아바타 6종(72px, 정서 축), 파스텔 스티커 18종
  (256px, 동작 축), SD 설정화 시트가 `backlog/assets/liv/` 에 있다.
- 본문은 건조한 보고서체가 기본이고 캐릭터 자아는 양념이다 — 한 번에 1-2문장, 페이지당 서너 번.
  드물수록 효과가 크다는 것이 확정된 방침이다(TASK-11).
- 사외로 나가는 글에는 em-dash 를 쓰지 않는다(저장소 내부 문서에는 제한 없음).

## Evidence on Hand

- 발행된 페이지 57건(2026 년도 아티클 + `p/` 지원 페이지). 실제 콘텐츠가 이미 사이트에 있다.
- 캐릭터 자산 실물: `backlog/assets/liv/`(아바타·스티커·설정화 시트), 브리핑 템플릿
  `backlog/assets/briefing/`.
- 결정 이력: `backlog/`(태스크·decisions·docs), 아카이브된 `PLAN.md`.
- 없는 것: 사용자 리서치, 방문자 인터뷰, 성과 지표 목표치. Cloudflare Web Analytics 대시보드에 트래픽
  집계가 있을 뿐 이 저장소에 수치는 없다. 지어내지 않는다.

## Product Principles

1. **한 페이지는 그 자체로 완결된다.** 링크 하나만 받은 독자가 다른 페이지를 열지 않고도 다 읽을 수 있어야
   하고, 파일 하나만 남아도 그대로 열려야 한다.
2. **의존은 수명이다.** 외부 폰트·CDN·차트 라이브러리를 쓰지 않는 이유는 취향이 아니라, 그것이 죽으면
   페이지가 죽기 때문이다.
3. **진실원본은 하나, 나머지는 생성물.** 같은 사실을 두 곳에 손으로 적지 않는다. 파생물은 스크립트가 만든다.
4. **독자의 시간을 아낀다.** 아는 사람을 상정하고 자명한 논거를 늘어놓지 않는다. 짧게, 아이디어 위주로.
5. **자아는 드물수록 세다.** 캐릭터는 보고서를 읽게 만드는 장치이지 보고서를 밀어내는 주인공이 아니다.

## Accessibility & Inclusion

명시적으로 채택한 표준 등급은 없다. 다만 구현이 실제로 지키고 있는 것: 라이트·다크 양쪽 본문 대비 확보,
모든 인터랙티브 요소에 `:focus-visible` 아웃라인, 삽화·스티커·아바타에 서술형 `alt`, 시맨틱 랜드마크
(`header`/`main`/`aside`/`footer`), 절 앵커의 터치 기기 폴백(`@media (hover: none)` 에서 상시 노출),
JS 없이도 읽히는 본문.
